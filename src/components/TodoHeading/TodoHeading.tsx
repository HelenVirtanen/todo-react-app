import React from 'react';
import styles from './TodoHeading.module.css';

const TodoHeading: React.FC = () => {
  return <h1 className={styles.todoHeading}>todos</h1>;
};

export default TodoHeading;
